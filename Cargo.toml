[workspace]
members = [
    "graphyne-core",
    "graphyne-server",
    "graphyne-client",
    "graphyne-cli",
    "graphyne-proto",
]

[workspace.dependencies]
# Core dependencies will be added as crates are created
