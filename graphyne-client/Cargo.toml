[package]
name = "graphyne-client"
version = "0.1.0"
edition = "2021"

[dependencies]
graphyne-proto = { path = "../graphyne-proto" }
tonic = "0.10"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
reqwest = { version = "0.11", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
http = "0.2"
